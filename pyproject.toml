[project]
name = "jobpilot"
version = "0.1.0"
description = "A learning-focused job application assistant built with the Model Context Protocol."
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]",
]

[dependency-groups]
dev = [
    "pytest",
]
